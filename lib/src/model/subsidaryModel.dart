class SubsidaryModel {
  String cinfo, data, name, web;

  SubsidaryModel(this.cinfo, this.data, this.name, this.web);
}

class Interest {
  String interest, loanInfo;

  Interest(this.interest, this.loanInfo);
}
